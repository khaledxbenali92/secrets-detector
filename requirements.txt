# Core — no heavy dependencies, runs anywhere
pathlib2>=2.3.7; python_version < "3.4"

# Optional — for enhanced features
colorama>=0.4.6      # Windows color support
click>=8.1.0         # Enhanced CLI (optional)
